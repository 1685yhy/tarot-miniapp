# 星光映照 v2.0 — 三大新功能实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task.

**Goal:** 在 v1.0.0 基础上新增每日一牌(牌意教学)、行动建议卡片、牌面故事分享三个差异化功能

**Architecture:** 前端新增 daily-card 页面替换首页每日卡牌区域；reading-result 增强为四段式(牌面→解读→建议→分享)；后端扩展 AI prompt 体系、新增卡牌教学内容的数据库表和API

**Tech Stack:** WeChat Miniapp (WXML/WXSS/JS), FastAPI (Python), DeepSeek API, SQLite

**Spec:** 基于调研结论——用户需要学塔罗(非被算命)、可行动建议(非只告知)、真实牌面(非AI幻觉)

## Global Constraints

- 微信小程序主包不超过 2MB
- 所有 AI 调用使用 DeepSeek API，超时 120s
- 新增页面必须在 app.json 注册
- CSS 变量必须使用 common.wxss 中定义的设计 token
- 禁止硬编码色值，禁止 emoji 作为功能图标
- 所有页面必须覆盖 loading/error/empty 三种状态
- 每个 Task 独立可测试，完成后可单独验证

---

## 文件结构

```
miniapp/pages/daily-card/          # [New] 每日一牌页面
  daily-card.js                    # 页面逻辑：加载/翻牌/日记关联
  daily-card.wxml                  # 卡牌展示 + 牌意教学 + 日记入口
  daily-card.wxss                  # 翻转动画 + 知识卡片样式
  daily-card.json                  # 页面配置

miniapp/pages/reading-result/      # [Modify] 增强解读结果页
  reading-result.js                # +行动建议渲染 + 牌面故事生成
  reading-result.wxml              # +建议区段 + 分享卡片
  reading-result.wxss              # +建议卡片样式

miniapp/pages/index/               # [Modify] 首页增强
  index.js                         # 每日一牌区域改为教学入口
  index.wxml                       # 每日一牌卡片替换为新组件
  index.wxss                       # 微调

miniapp/components/action-cards/   # [New] 行动建议组件
  action-cards.js                  # 勾选/取消/分享逻辑
  action-cards.wxml                # 待办卡片列表
  action-cards.wxss                # 卡片 + 勾选动画
  action-cards.json

miniapp/components/share-poster/   # [New] 分享海报组件
  share-poster.js                  # Canvas 绘制
  share-poster.wxml                # 海报预览 + 保存按钮
  share-poster.wxss
  share-poster.json

miniapp/utils/canvas-poster.js     # [New] Canvas 海报绘制工具

backend/app/models/card_teaching.py # [New] 卡牌教学内容模型
backend/app/api/cards.py           # [Modify] +教学接口
backend/app/api/readings.py        # [Modify] +建议prompt + 牌面分析
backend/app/services/ai_engine.py  # [Modify] +教学prompt + 建议prompt
backend/app/db/                    # [Modify] +教学数据迁移
```

---

## Task 0: 卡牌教学数据准备

**Files:**
- Create: `backend/app/models/card_teaching.py`
- Create: `backend/scripts/seed_card_teaching.py`
- Modify: `backend/app/db/database.py`

**What it does:** 为每张卡牌准备教学数据——牌面符号解读、历史典故、实用关键词

**验收标准:**
- [ ] 78张牌各有完整教学数据：符号(3-5个)、典故(1段)、关键词(3-5个)
- [ ] 数据通过 API 可查询：`GET /cards/{id}/teaching`
- [ ] 大阿尔卡纳22张有扩展教学(700+字)，小阿尔卡纳56张有基础教学(300+字)

---

## Task 1: 每日一牌页面

**Files:**
- Create: `miniapp/pages/daily-card/daily-card.js`
- Create: `miniapp/pages/daily-card/daily-card.wxml`
- Create: `miniapp/pages/daily-card/daily-card.wxss`
- Create: `miniapp/pages/daily-card/daily-card.json`
- Modify: `miniapp/app.json`

**What it does:** 每天自动翻一张牌，教用户这张牌的含义、符号、典故

**验收标准:**
- [ ] 页面加载显示卡牌翻转动画(1.5s 翻转 + 0.3s 金色光芒)
- [ ] 卡牌正反面：正面=牌面大图，背面=金色花纹图案
- [ ] 教学区域分三段：符号解读(3-5个视觉元素标注)、典故故事(1段)、生活关联(1句话)
- [ ] 底部"今日心情"日记入口：输入框 + 保存按钮，保存到 diary
- [ ] 顶部进度条：22张大阿尔卡纳收集进度 (X/22)
- [ ] 每天只能翻一次，0点重置
- [ ] 已翻过的牌在收集墙中显示(缩略图+日期)
- [ ] Loading: 卡牌翻转骨架屏；Error: "星光暂时无法连接" + 重试；Empty: 无

---

## Task 2: 首页每日一牌改造

**Files:**
- Modify: `miniapp/pages/index/index.js`
- Modify: `miniapp/pages/index/index.wxml`

**What it does:** 首页每日卡牌区域改为教学入口，点击进入 daily-card 页面

**验收标准:**
- [ ] 首页每日卡牌区域：显示今日卡牌缩略图 + 牌名 + "点击学习这张牌"
- [ ] 点击跳转到 daily-card 页面（带今日卡牌 ID）
- [ ] 如果今天已翻过，显示"已学习"标识
- [ ] 未翻过时显示呼吸动画吸引点击

---

## Task 3: AI 行动建议系统

**Files:**
- Modify: `backend/app/services/ai_engine.py`
- Modify: `backend/app/api/readings.py`

**What it does:** AI 解读后附带 3 条具体行动建议，格式化为可执行清单

**验收标准:**
- [ ] AI prompt 扩展：在解读末尾要求输出 3 条行动建议，格式 `[ACTION]建议内容[/ACTION]`
- [ ] 建议从 AI 回复中解析为结构化数组：`[{id, content, category}]`
- [ ] 三种分类：love→心形图标, career→公文包图标, general→星光图标
- [ ] 后端响应新增 `action_items` 字段，与 `interpretation` 同级
- [ ] 建议写入选用的(避免重复建议)

---

## Task 4: 行动建议卡片组件 + 结果页集成

**Files:**
- Create: `miniapp/components/action-cards/action-cards.js`
- Create: `miniapp/components/action-cards/action-cards.wxml`
- Create: `miniapp/components/action-cards/action-cards.wxss`
- Create: `miniapp/components/action-cards/action-cards.json`
- Modify: `miniapp/pages/reading-result/reading-result.json`
- Modify: `miniapp/pages/reading-result/reading-result.wxml`
- Modify: `miniapp/pages/reading-result/reading-result.js`

**What it does:** 解读结果页底部展示 3 条可勾选的行动建议卡片

**验收标准:**
- [ ] 3 条建议以卡片形式展示，每条一行，左侧勾选框
- [ ] 点击勾选：卡片变暗 + 勾选动画(0.3s ease-out)
- [ ] 点击已勾选：取消勾选
- [ ] 底部统计："已完成 X/3"
- [ ] 全部完成后显示庆祝动画：三颗星依次亮起
- [ ] 建议保存到本地 Storage，下次打开同一 reading 可恢复状态
- [ ] Loading: 骨架卡片；Error: 不阻断（建议区域失败不影响解读显示）；Empty: 不展示建议区域

---

## Task 5: 牌面故事 — AI 看图解读

**Files:**
- Modify: `backend/app/services/ai_engine.py`
- Modify: `backend/app/api/readings.py`

**What it does:** AI 在解读中引用卡牌视觉元素，证明"看到了"真实的牌

**验收标准:**
- [ ] AI prompt 注入卡牌的 image_description 字段（每张牌有视觉描述）
- [ ] 解读中至少引用 1 处牌面具体元素（如"你抽到的星币侍卫，画面上年轻人双手捧着一枚星币..."）
- [ ] 正逆位时引用不同的视觉元素
- [ ] 所有 78 张牌的 image_description 字段补全（如数据缺失，生成兜底描述）

---

## Task 6: 分享海报组件

**Files:**
- Create: `miniapp/utils/canvas-poster.js`
- Create: `miniapp/components/share-poster/share-poster.js`
- Create: `miniapp/components/share-poster/share-poster.wxml`
- Create: `miniapp/components/share-poster/share-poster.wxss`
- Create: `miniapp/components/share-poster/share-poster.json`
- Modify: `miniapp/pages/reading-result/reading-result.json`
- Modify: `miniapp/pages/reading-result/reading-result.wxml`

**What it does:** 将牌面+解读生成一张精美海报，可保存到相册或分享

**验收标准:**
- [ ] Canvas 绘制海报：上方牌面大图(居中, 700rpx宽)，中间解读精华(150字内)，下方小程序码+星光映照logo
- [ ] 海报背景：深靛蓝渐变 + 金色边框装饰
- [ ] "保存到相册"按钮：调起微信保存授权
- [ ] "分享给朋友"按钮：调起微信分享
- [ ] 绘制耗时 < 2s
- [ ] Loading: 绘制中提示；Error: 保存失败提示 + 重试

---

## Task 7: 集成测试 + 端到端验证

**Files:**
- Modify: `docs/ACCEPTANCE.md` (新增 v2.0 验收项)

**What it does:** 全流程验证三个新功能

**验收标准:**
- [ ] 每日一牌：首页 → 翻牌 → 学习 → 写心情 → 收集墙 全流程无异常
- [ ] 行动建议：解读 → 查看建议 → 勾选 → 全部完成 → 庆祝动画 全流程无异常
- [ ] 牌面故事：解读中出现具体牌面元素引用（肉眼可识别）
- [ ] 分享海报：生成 → 预览 → 保存 全流程无异常
- [ ] 首页回归：不改动现有 flow，原有功能正常工作
- [ ] 6/6 原有页面回归全绿，0 异常
- [ ] 所有新页面 3 状态覆盖(loading/error/empty)
