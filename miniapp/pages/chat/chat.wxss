/* ============================================================
   Chat Page — "A Quiet Conversation Under the Stars"
   Intimate late-night follow-up about a tarot reading
   ============================================================ */

page {
  font-family: -apple-system, BlinkMacSystemFont, 'Helvetica Neue',
               'PingFang SC', 'Microsoft YaHei', sans-serif;
}

/* ================================================================
   Container
   ================================================================ */
.container {
  padding: 32rpx 32rpx 260rpx;
  min-height: 100vh;
  display: flex;
  flex-direction: column;
  position: relative;
  z-index: 1;
  box-sizing: border-box;
}

/* ================================================================
   Animations — message entrance
   ================================================================ */
@keyframes fadeIn {
  from { opacity: 0; }
  to   { opacity: 1; }
}

@keyframes msgSlideUp {
  0%   { opacity: 0; transform: translateY(20rpx); }
  100% { opacity: 1; transform: translateY(0); }
}

@keyframes slideFadeRight {
  0%   { opacity: 0; transform: translateX(24rpx); }
  100% { opacity: 1; transform: translateX(0); }
}

@keyframes slideFadeLeft {
  0%   { opacity: 0; transform: translateX(-24rpx); }
  100% { opacity: 1; transform: translateX(0); }
}

.msg-enter {
  animation: msgSlideUp 0.5s cubic-bezier(0.22, 1, 0.36, 1) both;
}

.msg-user .msg-bubble {
  animation: slideFadeRight 0.5s cubic-bezier(0.22, 1, 0.36, 1) both;
}

.msg-ai .msg-bubble {
  animation: slideFadeLeft 0.5s cubic-bezier(0.22, 1, 0.36, 1) both;
}

/* ================================================================
   Skeleton loading
   ================================================================ */
.skeleton-wrap {
  flex: 1;
  padding: 20rpx 0;
}

.skeleton-block {
  margin-bottom: 40rpx;
}

.skeleton-line {
  height: 20rpx;
  border-radius: var(--radius-pill);
  background: linear-gradient(90deg, var(--color-surface) 25%, #1e1a38 50%, var(--color-surface) 75%);
  background-size: 200% 100%;
  animation: shimmer 1.6s infinite;
  margin-bottom: 12rpx;
}

.skeleton-bubble {
  height: 68rpx;
  border-radius: var(--radius-lg);
  background: linear-gradient(90deg, var(--color-surface) 25%, #1e1a38 50%, var(--color-surface) 75%);
  background-size: 200% 100%;
  animation: shimmer 1.6s infinite;
}

.skeleton-bubble.left  { width: 60%; }
.skeleton-bubble.right { width: 50%; margin-left: auto; }

@keyframes shimmer {
  0%   { background-position: 200% 0; }
  100% { background-position: -200% 0; }
}

/* ================================================================
   Context header — tiny lavender reminder
   ================================================================ */
.ctx-header {
  padding: 0 4rpx 28rpx;
  text-align: center;
}

.ctx-label {
  font-size: 22rpx;
  color: var(--color-lavender);
  letter-spacing: 2rpx;
  line-height: 1.5;
  display: block;
}

.ctx-question {
  font-size: 20rpx;
  color: #9088B0;
  display: block;
  margin-top: 4rpx;
  line-height: 1.4;
  /* clamp to 1 line with ellipsis */
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
  max-width: 70%;
  margin-left: auto;
  margin-right: auto;
}

/* ================================================================
   Empty state — warm invitation
   ================================================================ */
.empty-wrap {
  flex: 1;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 60rpx 40rpx 120rpx;
}

.empty-inner {
  text-align: center;
  position: relative;
}

.empty-glow {
  width: 120rpx;
  height: 120rpx;
  border-radius: 50%;
  background: radial-gradient(circle, var(--color-gold-glow) 0%, transparent 70%);
  margin: 0 auto 36rpx;
  animation: breath 3s ease-in-out infinite;
}

@keyframes breath {
  0%, 100% { transform: scale(0.95); opacity: 0.6; }
  50%      { transform: scale(1.05); opacity: 1; }
}

.empty-heading {
  display: block;
  font-size: 30rpx;
  color: #E8E0D4;
  line-height: 1.7;
  letter-spacing: 2rpx;
  margin-bottom: 16rpx;
}

.empty-sub {
  display: block;
  font-size: 24rpx;
  color: var(--color-lavender);
  line-height: 1.6;
  letter-spacing: 1rpx;
}

/* ================================================================
   Error state
   ================================================================ */
.error-state {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 120rpx 40rpx;
  text-align: center;
}

.error-icon {
  font-size: 48rpx;
  color: var(--color-gold-glow);
  margin-bottom: 24rpx;
  display: block;
}

.error-text {
  font-size: 30rpx;
  color: #E8E0D4;
  margin-bottom: 12rpx;
  display: block;
}

.error-hint {
  font-size: 24rpx;
  color: #9088B0;
  margin-bottom: 40rpx;
  display: block;
  line-height: 1.5;
}

.retry-btn {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  padding: 18rpx 44rpx;
  border: none;
  border-radius: var(--radius-pill);
  font-size: 26rpx;
  color: var(--color-bg);
  background: linear-gradient(135deg, var(--color-gold-light), var(--color-gold));
  box-shadow: 0 4rpx 20rpx var(--color-gold-glow-strong);
  transition: all 0.25s;
}

.retry-btn::after { border: none; }
.retry-btn:active { transform: scale(0.96); }

/* ================================================================
   Message list
   ================================================================ */
.msg-list {
  flex: 1;
}

.msg-wrap {
  margin-bottom: 36rpx;
  display: flex;
  flex-direction: column;
}

.msg-user {
  align-items: flex-end;
}

.msg-ai {
  align-items: flex-start;
}

/* ================================================================
   Role labels
   ================================================================ */
.role-label {
  font-size: 20rpx;
  letter-spacing: 2rpx;
  margin-bottom: 8rpx;
  display: block;
  padding: 0 6rpx;
}

.role-user {
  color: var(--color-lavender);
}

.role-ai {
  color: var(--color-gold);
}

.role-typing {
  color: var(--color-lavender);
}

/* ================================================================
   Message bubbles
   ================================================================ */
.msg-bubble {
  padding: 22rpx 28rpx;
  max-width: 82%;
  position: relative;
  line-height: 1.7;
}

.msg-text {
  white-space: pre-wrap;
  word-break: break-word;
}

/* ---- User bubble: compact, warm dark card, gold left-accent ---- */
.bubble-user {
  background: var(--color-surface);
  border-radius: 18rpx 4rpx 18rpx 18rpx;
  border: 1rpx solid rgba(255, 255, 255, 0.03);
  border-left: 2rpx solid var(--color-gold-glow);
  box-shadow: 0 2rpx 12rpx rgba(0, 0, 0, 0.18);
}

.msg-text-user {
  font-size: 26rpx;
  color: var(--color-text-primary);
  line-height: 1.6;
}

/* ---- AI bubble: lighter card, gold left-margin line ---- */
.bubble-ai {
  background: var(--color-card);
  border-radius: 4rpx 18rpx 18rpx 18rpx;
  border: 1rpx solid rgba(244, 212, 140, 0.08);
  border-left: 3rpx solid var(--color-gold);
  box-shadow: 0 2rpx 20rpx rgba(212, 175, 55, 0.06);
}

.msg-text-ai {
  font-size: 28rpx;
  color: var(--color-text-primary);
  line-height: 1.7;
}

/* ================================================================
   Typing indicator — pulsing gold dots
   ================================================================ */
.typing-box {
  display: flex;
  align-items: center;
  gap: 10rpx;
  background: var(--color-card);
  border-radius: 4rpx 18rpx 18rpx 18rpx;
  border: 1rpx solid rgba(244, 212, 140, 0.08);
  border-left: 3rpx solid var(--color-gold);
  padding: 26rpx 32rpx;
  min-height: 28rpx;
}

.typing-dot {
  width: 14rpx;
  height: 14rpx;
  border-radius: 50%;
  background: linear-gradient(135deg, var(--color-gold-light), var(--color-gold));
  animation: typingPulse 1.4s ease-in-out infinite;
}

.typing-dot:nth-child(2) { animation-delay: 0.2s; }
.typing-dot:nth-child(3) { animation-delay: 0.4s; }

@keyframes typingPulse {
  0%, 80%, 100% {
    transform: translateY(0) scale(0.7);
    opacity: 0.35;
  }
  40% {
    transform: translateY(-8rpx) scale(1);
    opacity: 1;
  }
}

/* ================================================================
   Input area — fixed at bottom
   ================================================================ */
.input-bar {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  display: flex;
  align-items: center;
  gap: 16rpx;
  padding: 20rpx 32rpx;
  padding-bottom: calc(env(safe-area-inset-bottom) + 20rpx);
  background: rgba(26, 26, 62, 0.97);
  border-top: 1.5rpx solid rgba(244, 212, 140, 0.08);
  z-index: 100;
  backdrop-filter: blur(12rpx);
  -webkit-backdrop-filter: blur(12rpx);
}

.input-wrap {
  flex: 1;
  display: flex;
}

.chat-input {
  width: 100%;
  height: 72rpx;
  background: #1e1a38;
  border-radius: var(--radius-pill);
  padding: 0 28rpx;
  font-size: 28rpx;
  color: var(--color-text-primary);
  border: 1.5rpx solid rgba(244, 212, 140, 0.08);
  transition: border-color 0.25s, box-shadow 0.25s;
  caret-color: var(--color-gold);
}

.chat-input:focus {
  border-color: var(--color-gold);
  box-shadow: 0 0 0 4rpx rgba(244, 212, 140, 0.10);
}

.input-placeholder {
  color: #8A85A8;
}

/* ---- Send button: small gold circle with arrow ---- */
.send-btn {
  width: 72rpx;
  height: 72rpx;
  min-width: 72rpx;
  min-height: 72rpx;
  border-radius: 50%;
  border: none;
  padding: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  background: linear-gradient(135deg, var(--color-gold-light), var(--color-gold));
  box-shadow: 0 4rpx 16rpx var(--color-gold-glow-strong);
  transition: all 0.25s;
  position: relative;
}

.send-btn::after { border: none; }

.send-btn:active:not([disabled]) {
  transform: scale(0.90);
}

.send-btn[disabled] {
  opacity: 0.3;
}

.send-icon {
  width: 0;
  height: 0;
  border-left: 12rpx solid var(--color-bg);
  border-top: 8rpx solid transparent;
  border-bottom: 8rpx solid transparent;
  margin-left: 4rpx;
}

/* ================================================================
   Free query hint
   ================================================================ */
.free-hint {
  text-align: center;
  padding: 12rpx 0;
  font-size: 20rpx;
  color: #9088B0;
  position: fixed;
  bottom: 106rpx;
  left: 0;
  right: 0;
  letter-spacing: 1rpx;
}

/* ================================================================
   Scrollbar hidden
   ================================================================ */
::-webkit-scrollbar {
  width: 0;
  height: 0;
}
